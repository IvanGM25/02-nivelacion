const nombre = 'pepe';
const real = 'guillermo';

const salida = nombre + ' '+ real;
const salida2 = `${nombre} ${real}`;

console.log(salida);
console.log(salida2);

console.log(salida === salida2) ;// true
